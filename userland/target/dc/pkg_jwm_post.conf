jwm_post() {
cat > $root/usr/bin/startx <<-EOT
#!/bin/sh
export DISPLAY=:0
Xfbdev :0 -shadow &
xpid=\$!

i=0
while [ ! -S /tmp/.X11-unix/X0 ] && [ "\$i" -lt 60 ]; do
    sleep 1
    i=\$((i + 1))
done

jwm
kill "\$xpid" 2>/dev/null

EOT
}

hook_add postinstall 5 jwm_post
